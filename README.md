# contracts

**Хаб данных** — хранилище канона, ассетов и UI-контрактов.

---

## Роль в экосистеме

| | |
|---|---|
| **Слой** | Данные |
| **Функция** | Хранилище universe.json, ассетов, UI-контрактов |
| **Входы** | Редактирование через editor.html |
| **Выходы** | Данные для dream-graph, godot-sandbox |

```
editor.html → universe.json → dream-graph (витрина)
                    ↓
              godot-sandbox (режиссура)
```

> contracts **не определяет структуру системы** — он только хранит и интерпретирует канон.

---

## Структура

```
contracts/public/
├── graph/
│   ├── universe.json    # 🎯 Канон — SSoT
│   └── editor.html      # Редактор графа
├── assets/
│   ├── logos/           # Логотипы
│   ├── avatars/         # Аватары персонажей
│   ├── icons/           # Иконки
│   ├── images/          # Изображения
│   ├── ui/              # UI элементы
│   └── flags/           # Флаги языков
├── texts/
│   ├── story/           # Нарративный контент
│   ├── system/          # Техническое описание
│   └── service/         # Описание действий
├── sessions/            # Состояния сессий (не канон)
├── ui/
│   ├── layout/          # Расположение панелей
│   ├── interaction/     # Правила взаимодействия
│   └── bindings/        # Связи элементов с ассетами
└── manifests/
    └── assets.manifest.json
```

---

## Главное: universe.json

Единственный источник истины для структуры системы.

```json
{
  "meta": { "version": "1.0" },
  "nodes": [
    {
      "id": "universe",
      "label": "Universe",
      "type": "root",
      "story": "Нарративный контент",
      "system": "Техническое описание",
      "service": "Доступные действия"
    }
  ],
  "edges": [
    { "id": "e1", "source": "universe", "target": "concept-a" }
  ]
}
```

---

## Быстрый старт

### Редактирование графа

1. Открыть `contracts/public/graph/editor.html` в браузере
2. Добавить/изменить узлы и рёбра
3. Заполнить Story/System/Service
4. Сохранить → файл скачается
5. Положить `universe.json` обратно в `contracts/public/graph/`

### Добавление ассетов

1. Положить файл в соответствующую папку `assets/`
2. Использовать путь в universe.json или текстах

---

## Структура vs Состояние

| Структура (канон) | Состояние (не канон) |
|-------------------|----------------------|
| `graph/universe.json` | `sessions/*` |
| Узлы, связи, контент | Camera, selection, UI |

Рендеры читают структуру, но не изменяют её.

---

## Связанные репозитории

| Репозиторий | Роль | Статус |
|-------------|------|--------|
| **extended-mind** | Ядро, редактор | Активный |
| **dream-graph** | Витрина — финальный рендер | Активный |
| **godot-sandbox** | Режиссура UI | Строительные леса |

---

## Лицензия

MIT © utemix-lab
